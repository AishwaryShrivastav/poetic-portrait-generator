
export interface UserData {
  name: string;
  designation: string;
  company: string;
  email: string;
}

export interface GeneratedResult {
  id: string;
  poem: string;
  portraitUrl: string;
  createdAt: string;
}
